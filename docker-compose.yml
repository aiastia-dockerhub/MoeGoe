version: '3.9'

services:
  openaibot:
    image: aiastia/moegoe:master
    command: python3 server.py
    volumes:
      - ./:/app
    networks:
      - app-tier
  

networks:
  app-tier:
    driver: bridge
